import styled from "styled-components";

export const SSelectItemCard = styled.li`
  width: 100%;
  line-height: 54px;
  font-size: 14px;
  background-color: ${({theme}) => theme.color.oftenBaseWhite};
  color: ${({theme}) => theme.color.oftenBlack};
`